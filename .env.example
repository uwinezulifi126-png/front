# Dev (recommended): leave unset — browser calls /api/* on the Vite host,
# and vite.config.ts proxies to http://127.0.0.1:8000 (works for LAN clients).
#
# Force a direct API base (skips Vite proxy; also update backend CORS):
# VITE_API_BASE_URL=http://10.161.8.252:8000
